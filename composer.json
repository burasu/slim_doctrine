{
    "require": {
        "slim/views": "^0.1.3",
        "doctrine/orm": "^2.5"
    },

    // Incluimos alternativamente la ruta de acceso
    // para la carga automatica depende de como se 
    // maneja la carga automática de la aplicación.
    "autoload": {
    	"psr-4": {
    		"App\\": "src/App"
    	}
    }
}
